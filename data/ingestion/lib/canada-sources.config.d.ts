// Type declaration for the hand-authored JS source config
// (canada-sources.config.js). The runtime value is an array of
// heterogeneous Canada source descriptors that run-canada consumes as
// `Src` (Record<string, any>) — see run-canada.ts. Types-only: the JS
// runtime is unchanged.
export declare const CANADA_SOURCES: Record<string, unknown>[];
