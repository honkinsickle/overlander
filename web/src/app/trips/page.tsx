/** /trips children slot. The trip list + chrome live in
 *  app/trips/layout.tsx so they stay mounted across /trips and
 *  /trips/[id]. This page exists to make /trips a valid route. */
export default function TripsIndexPage() {
  return null;
}
